# Maven Core Module

Use this module for Maven-based JVM project review: POM structure, dependency management,
plugin configuration, Spring Boot Maven plugin usage, test lifecycle, packaging, profiles,
and reproducible builds.

Start with:

- `references/maven-core.md`

This module is intentionally smaller than the Java, Kotlin, Ktor, and Spring modules
because the repository does not yet include a dedicated Maven reference corpus.
