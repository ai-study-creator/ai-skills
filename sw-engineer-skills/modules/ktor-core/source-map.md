# Source Map

Primary source corpus:

- `knowledge/ktor/ktor-referece-md`

Coverage intent:

- High practical coverage because the Ktor corpus is moderate in size.
- Coroutine-specific behavior should be cross-checked with `modules/kotlin-coroutines`.
