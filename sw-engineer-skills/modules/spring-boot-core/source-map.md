# Source Map

Primary source corpus:

- `knowledge/springboot/springboot-reference-md`

Coverage intent:

- Practical Spring Boot backend coverage.
- Keeps reactive WebFlux depth in `modules/spring-webflux`.
- Keeps examples curated so skills do not load the full Spring Boot corpus by default.
