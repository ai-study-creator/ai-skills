# Maven Knowledge Placeholder

This folder is reserved for a dedicated Maven reference corpus.

Current Maven coverage is sourced indirectly from Spring Boot and Kotlin references plus
the curated `modules/maven-core` module. Treat Maven adherence as practical but incomplete
until official Maven documentation or another dedicated Maven corpus is added here.
