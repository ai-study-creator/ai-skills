# Source Map

Primary source corpus:

- `knowledge/kotlin/kotlin-reference-md`

Coverage intent:

- High practical coverage because the Kotlin corpus is small enough to distill more fully.
- Coroutine depth lives in `modules/kotlin-coroutines`.
